# Backlog

구현 예정 기능 및 개선 사항 목록.

---

## UX / 편의성

- [x] 스캔 결과 요약 표시 — 탭별 그룹 수, 절약 가능 용량 상단 표시
- [x] 그룹 목록 전체 정렬 옵션 — 절약 용량순 / 최대 파일 크기순 / 파일 수순
- [x] KEEP/REMOVE 실시간 통계 표시 — "REMOVE 선택: N개, X.X GB 절약 예정"
- [x] 다크/라이트 테마 토글
- [x] ZIP 불러오기 후 파일 존재 확인 전까지 스캔 시작 버튼 비활성화 + 마우스오버 툴팁 안내
- [x] 전체 초기화 버튼 — 스캔 기록·CSV 기록 전부 초기화
- [x] 일반 탭 아카이브 내부 항목 필터 — 전체 / 일반 파일만 / 아카이브 내부만
- [x] 일반 탭 아카이브 우선 정렬

## UX / 편의성 (추가)

- [ ] 스캔 경로 행 추가 UI — textarea 대신 행 목록으로 교체. + 버튼으로 행 추가, 📂 버튼으로 폴더 선택 다이얼로그(Tauri dialog.open), ✕ 버튼으로 행 삭제
- [ ] 스캔 진행 카운터 — "해시 중복 탐지 중... (1,203 / 5,000)" 형태로 같은 줄 덮어쓰기. Rust에서 `\r` 마커로 덮어쓰기 신호 전송, JS에서 마지막 진행 줄 교체 처리
- [ ] 파일 타입별 통계 패널 — 스캔 완료 후 일반/이미지/영상/아카이브별 그룹 수·파일 수·절약 가능 용량 표시. SVG 도넛 차트(외부 라이브러리 없이 순수 SVG)로 타입별 비율 시각화
- [ ] 이미지 그룹 내 diff 뷰 — 그룹 내 두 파일을 나란히 비교하는 모달. 이미지는 img 태그 나란히 표시, 파일 크기·해상도·수정일 메타 비교
- [ ] 아카이브 탭 일치도순 정렬 — 공통 파일 수(shared) 기준 내림차순 정렬 옵션 추가
- [ ] 스캔 프리셋 — 경로·제외패턴·임계값 등 옵션 조합을 이름 붙여 저장·불러오기. localStorage 기반

## 안전성

- [ ] 삭제 전 휴지통 이동 옵션 — macOS: `trash`, Linux: `gio trash`
- [x] 삭제 이력 로그 파일 저장 — 언제 무엇을 삭제했는지 기록

## 성능

- [ ] CSV 캐시 기반 해시 스킵 — 이전 ZIP의 path + size 일치 시 hash/phash/vhash 재계산 생략
- [x] 탭 전환 성능 개선 — VirtualScroller(뷰포트 기준 DOM 최소화) + TabCache 적용. 2만 개 이상 그룹 대응

## 기능

- [x] 열기 버튼 OS 분기 — macOS: `open -R`, Windows: `explorer /select,`, Linux: `xdg-open`
- [ ] 그룹 내 원본 추천 — 가장 오래된 파일 또는 경로 깊이 기준으로 KEEP 후보 표시
- [x] 스캔 제외 패턴 — 고급 옵션에 exclude glob 패턴 입력 추가
- [x] 아카이브 탭 압축파일 내부 파일 수 표시 — 그룹 헤더에 각 압축파일이 포함한 파일 수 표시
- [x] 파일 존재 확인 로그 간소화 — "확인 시작 → 완료 (없는 파일 N개)" 요약만 출력

## 버그 수정

- [x] 파일 존재 확인 시 zip_entry 경로 오판 — `::` 앞 zip 파일 기준으로 존재 확인
- [x] 열기 버튼 특수문자/공백 포함 경로 동작 안 함 — data-path 방식으로 교체
- [x] zip_entry 삭제 시 건너뜀 처리

## 추후 구현

- [ ] 스캔 진행률 게이지 — SSE(Server-Sent Events) 또는 WebSocket으로 폴링 제거. 텍스트 로그 대신 단계별 프로그레스 바로 표시. Windows WebView2 폴링 스로틀링 문제 근본 해결



- [x] Tauri + Rust 완전 포팅 — Python 백엔드를 Rust로 재작성 (Axum or Actix-web), ffmpeg-next 정적 링크로 외부 의존성 제거, 단일 바이너리 배포. fetch → invoke 전환
- [ ] pHash 버킷 분류 후 BK-tree 적용 — pHash 상위 비트로 버킷 분류 → 버킷별 소규모 BK-tree 구성으로 모수 축소. 인접 버킷 교차 비교 처리 필요. 대규모 이미지 스캔 성능 개선 목적
- [ ] 부분 해시 기반 사전 필터링 — 앞 N KB + 뒤 N KB 해시로 1·2단계 후보군 축소 후 전체 해시(3단계) 확인. 사용자 옵션(`--partial-hash 64`)으로 노출하거나 full hash 스캔의 사전 작업으로 자동 적용. 대용량 파일·압축파일 스캔 I/O 대폭 절감 목적
- [ ] 같은 zip 내부끼리만 중복인 그룹 일반 탭에서 제거 — 해시 계산은 전체 모수 유지, 그룹화 결과에서 모든 파일이 동일 zip 출처인 그룹만 필터링. 다른 zip 또는 일반 파일과 섞인 그룹은 유지
- [ ] 해시 진입 전 모수 축소 옵션 — (1) 파일 크기 하한선(`--min-size N KB`): 지정 크기 미만 파일 해시 제외. (2) inode 중복 감지(`--check-inode`): 같은 inode 파일은 하드링크로 판정, 해시 계산 생략. 둘 다 고급 옵션 UI로 노출
